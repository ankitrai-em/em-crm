PORT=8787

# Signing secret for login sessions. REQUIRED in production — generate one with
# `openssl rand -hex 32` and never commit the real value.
JWT_SECRET=

# Where uploaded invoice files are stored. Defaults to server/uploads if unset.
# Point this at a mounted volume in production (see server/README.md).
# UPLOADS_DIR=./uploads

# mock (default, no credentials needed) | sarv | twilio | exotel
TELEPHONY_PROVIDER=mock

# --- Sarv / DeepCall (used when TELEPHONY_PROVIDER=sarv) ---
# SARV_USER_ID=
# SARV_TOKEN=

# --- Twilio (used when TELEPHONY_PROVIDER=twilio) ---
# TWILIO_ACCOUNT_SID=
# TWILIO_AUTH_TOKEN=
# TWILIO_FROM_NUMBER=
# TWILIO_TWIML_URL=

# --- Exotel (used when TELEPHONY_PROVIDER=exotel) ---
# EXOTEL_SID=
# EXOTEL_API_KEY=
# EXOTEL_API_TOKEN=
# EXOTEL_CALLER_ID=
# EXOTEL_AGENT_NUMBER=
# EXOTEL_SUBDOMAIN=api.exotel.com
